import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <button className="btn btn-primary w-64 rounded">Get Feeds</button>
    </div>
  );
};

export default Home;
